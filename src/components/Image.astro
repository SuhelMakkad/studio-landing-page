---
import type { HTMLAttributes } from "astro/types";

export type Props = { rounded?: boolean } & HTMLAttributes<"img">;

const { rounded = true, class: className, ...attrs } = Astro.props;
---

<img class={`${rounded ? "rounded-lg" : ""} ${className}`} {...attrs} />
