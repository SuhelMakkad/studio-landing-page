---
import type { HTMLAttributes } from 'astro/types';

export type Props = HTMLAttributes<"h1">

const {class: className, ...attrs} = Astro.props; 
---
<h1 {...attrs} class={`text-6xl font-bold ${className}`}>
  <slot />
</h1>
